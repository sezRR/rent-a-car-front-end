import { MaintenanceDirective } from './maintenance.directive';

describe('MaintenanceDirective', () => {
  it('should create an instance', () => {
    const directive = new MaintenanceDirective();
    expect(directive).toBeTruthy();
  });
});
